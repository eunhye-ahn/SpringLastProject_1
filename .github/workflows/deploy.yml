name:  
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: self-hosted
  defults:
    run:
      working-directory: SpringLastProject_1
      
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Maven Build
        run: mvn clean package -DskipTests
        
